<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="description" content="Erasmus+ Projects for PPMG 'akad. Ivan Tsenov' Vratsa (2024–2027)" />
    <meta name="author" content="PPMG 'akad. Ivan Tsenov'" />
    <title>Erasmus+ Projects | PPMG 'akad. Ivan Tsenov'</title>
    <link rel="icon" type="image/x-icon" href="assets/favicon.ico" />
    <!-- Font Awesome icons (free version)-->
    <script src="https://use.fontawesome.com/releases/v6.3.0/js/all.js" crossorigin="anonymous"></script>
    <!-- Google fonts-->
    <link href="https://fonts.googleapis.com/css?family=Varela+Round" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css?family=Nunito:200,300,400,600,700,800,900" rel="stylesheet" />
    <!-- Core theme CSS (includes Bootstrap)-->
    <link href="css/styles.css" rel="stylesheet"/>
</head>
<body id="page-top">
    <!-- Navigation-->
    <nav class="navbar navbar-expand-lg navbar-light fixed-top" id="mainNav">
        <img src="../assets/img/images.png" alt="Logo" class="img-fluid shrink-to-fit mx-3" style="max-width: 65px; max-height: 65px; z-index: 0;">
        <div class="container px-4 px-lg-5">
            <a class="navbar-brand nav-link-active" href="#page-top">PPMG 'akad. Ivan Tsenov'</a>

            <!-- Translate button -->
            <button id="translateBtn" class="btn btn-outline-light ms-2" type="button" title="Translate to Bulgarian">BG</button>

            <button class="navbar-toggler navbar-toggler-right" type="button" data-bs-toggle="collapse" data-bs-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
                Menu <i class="fas fa-bars"></i>
            </button>
            <div class="collapse navbar-collapse" id="navbarResponsive">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="#about">About Us</a></li>
                    <li class="nav-item"><a class="nav-link" href="#projects">Erasmus+ Projects</a></li>
                    <li class="nav-item"><a class="nav-link" href="/news">News</a></li>
                    <li class="nav-item"><a class="nav-link" href="#contact">Contact</a></li>
                </ul>
            </div>
        </div>
    </nav>
    <!-- Masthead-->
    <header class="masthead">
        <div class="container px-4 px-lg-5 d-flex h-100 align-items-center justify-content-center">
            <div class="d-flex justify-content-center text-center">
                <div>
                    <h1 class="mx-auto my-0 text-uppercase" style="user-select: none;">Erasmus+ Projects</h1>
                    <h2 class="text-white-50 mx-auto mt-5 mb-5">Discover our international collaborations from 2024 to 2027</h2>
                    <a class="btn btn-primary" href="#projects">Explore Projects</a>
                </div>
            </div>
        </div>
    </header>

    <!-- About-->
    <section class="about-section text-center" id="about">
        <div class="container px-4 px-lg-5">
            <div class="row gx-4 gx-lg-5 justify-content-center pb-5 ">
                <div class="col-lg-8 pb-5">
                    <h2 class="text-white mb-4">About PPMG 'akad. Ivan Tsenov'</h2>
                    <p class="text-white-50">
                        PPMG 'akad. Ivan Tsenov' is a renowned school in Vratsa, Bulgaria. We are proud to participate in the Erasmus+ program, fostering international collaboration and providing our students with opportunities to engage in educational projects across Europe.
                    </p>
                    <button class="btn btn-primary mb-5"><a class="text-light text-decoration-none" href="/#about">Learn More</a></button>
                </div>
            </div>
        </div>
    </section>
    <!-- Projects-->
    <section class="projects-section bg-light" id="projects">
        <div class="container px-4 px-lg-5">
            <div class="text-center mb-5">
                <h2 class="text-black mb-4">Erasmus+ Projects (2024–2027)</h2>
                <p class="text-black-50">Explore our exciting projects and collaborations with schools across Europe.</p>
            </div>
            <!-- Project Cards-->
            <div class="row">
                <div class="col-md-6 mb-4">
                    <div class="card">
                        <img class="card-img-top" src="assets/img/project1.jpg" alt="Project 1">
                        <div class="card-body">
                            <h4 class="card-title">Project Title 1</h4>
                            <p class="card-text">A brief description of this project's goals, participants, and outcomes.</p>
                        </div>
                    </div>
                </div>
                <div class="col-md-6 mb-4">
                    <div class="card">
                        <img class="card-img-top" src="assets/img/project2.jpg" alt="Project 2">
                        <div class="card-body">
                            <h4 class="card-title">Project Title 2</h4>
                            <p class="card-text">A brief description of this project's goals, participants, and outcomes.</p>
                        </div>
                    </div>
                </div>
                <a class="text-light text-decoration-none mt-4 mx-auto text-center" href="/projects"><button class="btn btn-primary w-25">See All</button></a>
                <!-- Add more projects as needed -->
            </div>
        </div>
    </section>
        <!-- Contact Section-->
        <section class="contact-section bg-dark" id="contact">
            <div class="container px-4 px-lg-5">
                <div class="row gx-4 gx-lg-5 justify-content-center">
                    <div class="hover-line col-md-4 mb-3">
                        <div class="card py-4 h-100">
                            <div class="card-body text-center" id="openLocation" style="cursor: pointer;">
                                <i class="fas fa-map-marker-alt text-primary mb-2"></i>
                                <h4 class="text-uppercase m-0">Location</h4>
                                <hr class="my-4 mx-auto" />
                                <div class="small text-black-50">PPMG "akad. Ivan Tsenov" Vratsa</div>
                            </div>
                        </div>
                    </div>
                        <div class="hover-line col-md-4 mb-3">
                            <div class="card py-4 h-100">
                                <div class="card-body text-center">
                                    <i class="fas fa-envelope text-primary mb-2"></i>
                                    <h4 class="text-uppercase m-0">Email</h4>
                                    <hr class="my-4 mx-auto" />
                                    <div class="small text-black-50"><a href="mailto:erasmus@ppmg-vratza.bg">erasmus@ppmg-vratza.bg</a></div>
                                </div>
                            </div>
                        </div>
                            <div class="hover-line col-md-4 mb-3">
                        <div class="card py-4 h-100">
                            <div class="card-body text-center">
                                <i class="fas fa-phone text-primary mb-2"></i>
                                <h4 class="text-uppercase m-0">Phone</h4>
                                <hr class="my-4 mx-auto" />
                                <div 
                                class="small text-black-50 "            
                                id="copyText" 
                                class="text-primary text-decoration-underline" 
                                style="cursor: pointer;" 
                                data-bs-toggle="tooltip" 
                                data-bs-placement="top" 
                                data-bs-title="Copied!">+359 92 624 726</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    <!-- Footer-->
    <footer class="footer bg-dark small text-center text-white-50">
        <div class="container px-4 px-lg-5">Copyright &copy; PPMG 'akad. Ivan Tsenov' 2024</div>
        <a href="/admin" class="container px-4 px-lg-5 link-dark">Login as Admin</a>
    </footer>

    <!-- hidden placeholder for Google Translate -->
    <div id="google_translate_element" style="display:none;"></div>

    <!-- Improved translate script (avoids re-initialization recursion / stack overflows) -->
 <!--   <script>
      (function () {
        // guards to avoid repeated initialization which can cause stack overflows
        if (window._gtSafeInit) return;
        window._gtSafeInit = true;

        // set googtrans cookie before loading script
        function setGoogTransCookie() {
          try {
            var cookieName = 'googtrans';
            var cookieValue = '/en/bg';
            var expires = new Date(Date.now() + 365 * 24 * 60 * 60 * 1000).toUTCString();
            // simple cookie (no domain) works for localhost and normal hosts
            document.cookie = cookieName + '=' + cookieValue + ';expires=' + expires + ';path=/';
          } catch (e) {
            // fail silently
            console.error('setGoogTransCookie error', e);
          }
        }

        // safe loader with single-load guards
        function loadGoogleTranslateOnce() {
          if (window._gtLoading || window._gtLoaded) return;
          window._gtLoading = true;

          // define the callback exactly once
          window.googleTranslateElementInit = function () {
            try {
              // ensure we don't re-run the init function
              if (window._gtInitCalled) return;
              window._gtInitCalled = true;

              // create the element (Google will replace it)
              new google.translate.TranslateElement({
                pageLanguage: 'en',
                includedLanguages: 'bg',
                autoDisplay: false
              }, 'google_translate_element');

              // After init, try to set the select to 'bg' (polling because the combo may appear later)
              var attempts = 0;
              var maxAttempts = 20;
              var poll = setInterval(function () {
                attempts++;
                try {
                  var sel = document.querySelector('select.goog-te-combo');
                  if (sel) {
                    sel.value = 'bg';
                    // newer browsers may need InputEvent instead of Event
                    var ev;
                    try { ev = new Event('change', { bubbles: true }); } catch (e) { ev = document.createEvent('HTMLEvents'); ev.initEvent('change', true, false); }
                    sel.dispatchEvent(ev);
                    clearInterval(poll);
                  } else if (attempts >= maxAttempts) {
                    clearInterval(poll);
                  }
                } catch (e) {
                  // keep polling until safe or max attempts reached
                }
              }, 250);
            } catch (err) {
              console.error('googleTranslateElementInit error', err);
            } finally {
              window._gtLoaded = true;
              window._gtLoading = false;
            }
          };

          // append script and set load handlers
          var s = document.createElement('script');
          s.src = 'https://translate.google.com/translate_a/element.js?cb=googleTranslateElementInit';
          s.async = true;
          s.onload = function () {
            window._gtLoaded = true;
            window._gtLoading = false;
          };
          s.onerror = function (e) {
            console.error('Failed to load Google Translate script', e);
            window._gtLoading = false;
          };
          document.head.appendChild(s);
        }

        // button hookup
        var btn = document.getElementById('translateBtn');
        if (btn) {
          btn.addEventListener('click', function () {
            // set cookie first then load script once
            setGoogTransCookie();
            loadGoogleTranslateOnce();
          }, { passive: true });
        }
      })();
    </script> -->

    <!-- Bootstrap core JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="js/scripts.js"></script>
    <script src="/js/translator.js"></script>
</body>
</html>
